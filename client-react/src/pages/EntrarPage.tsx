import React,{useState}from'react';
import{useAutenticacao}from'../context/AutenticacaoContext';
export const EntrarPage:React.FC=()=>{const{entrar,erroAcesso}=useAutenticacao();const[email,setEmail]=useState('');const[senha,setSenha]=useState('');const[erro,setErro]=useState(erroAcesso);const[ocupado,setOcupado]=useState(false);const enviar=async(e:React.FormEvent)=>{e.preventDefault();setOcupado(true);setErro('');try{await entrar(email,senha)}catch(x:any){setErro(x.message||'Não foi possível entrar.')}finally{setOcupado(false)}};return <main className="login-page"><section className="login-card"><div className="login-brand"><span>🎯</span><div><h1>Chega Junto Concurseiro</h1><p>Organize sua preparação com acompanhamento individual.</p></div></div><form onSubmit={enviar}><label><span>E-mail</span><input className="form-control" type="email" autoComplete="username" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label><span>Senha</span><input className="form-control" type="password" autoComplete="current-password" required value={senha} onChange={e=>setSenha(e.target.value)}/></label>{erro&&<div className="auth-error">{erro}</div>}<button className="btn-primary" disabled={ocupado}>{ocupado?'Entrando…':'Entrar'}</button></form><small>A recuperação de senha será disponibilizada posteriormente.</small></section></main>};
